apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: echofs

resources:
  - namespace.yaml
  - postgres.yaml
  - orchestrator.yaml
  - storage-node.yaml
  # Decomposed services (Phase 2)
  - gateway.yaml
  - ingest.yaml
  - metadata-svc.yaml
  # Legacy monolith (can be removed once decomposed services are stable)
  # - master.yaml

commonLabels:
  app.kubernetes.io/part-of: echofs
  app.kubernetes.io/managed-by: kustomize
